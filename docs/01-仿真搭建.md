# 仿真搭建与模型校验

> 上游：LEAP_Hand_Sim / mujoco_menagerie

## 目标

在 MuJoCo 中搭出与真机**自由度一致**的灵巧手场景，用于抓取动作的预演验证。

## 环境

| 项 | 参考值 / 建议 | 来源 | 我的实测 |
|---|---|---|---|
| MuJoCo | 随 LEAP_Hand_Sim 依赖安装 | LEAP_Hand_Sim 仓库 | 待补 |
| Python | 与上游 requirements 一致 | 同上 | 待补 |
| mujoco_menagerie | 提供模型与场景资源 | google-deepmind | 待补 |
| SDK 接口 | Python / C++ / ROS / ROS 2 | LEAP Hand 官方规格 | Python |

## 硬件参考规格

对齐模型时我按官方规格逐项核对，参数见仓库 README 的「硬件规格」一节。其中最容易出问题的是**自由度数**：官方标称 16 个全部直接驱动，但 URDF 里若用 `<mimic>` 描述从动关系，导入 MJCF 后就会对不上，所以这一步必须数一遍而不是相信标称值。

## 模型校验清单

| 检查项 | 为什么重要 | 结果 |
|---|---|---|
| 关节限位 | 超出真机限位的动作根本发不下去 | 待补 |
| 速度上限 | 仿真能做到的速度真机不一定做得到 | 待补 |
| 惯量与质心 | 影响力矩计算与接触响应 | 待补 |
| 碰撞模型 | 碰撞体偏差会导致穿透或假接触 | 待补 |
| mimic 从动关节 | URDF 有、MJCF 没有 → 自由度数不一致 | 待补 |

## 自由度对齐

URDF 中四指 distal 常带 `<mimic>`，MJCF 无对应机制。处理方式：明确真机可主动控制的自由度 → 仿真侧补全执行器 → 锁定从动关节。

校验方式很朴素：**数一遍**仿真可控自由度数，必须等于真机可下发指令数。我第一次就是漏了这一步，动作在真机上完全对不上，回头查才发现差了几个自由度。

主动 / 从动分布：待补

## 抓取预演

| 物体 | 抓取方式 | 仿真结果 |
|---|---|---|
| 待补 | 捏取 | 待补 |
| 待补 | 掌握 | 待补 |

## 一点体会

仿真预演的价值不是"证明能抓起来"，而是提前暴露限位冲突和碰撞问题，省下真机试错的时间。真机时间比 GPU 时间贵得多。
